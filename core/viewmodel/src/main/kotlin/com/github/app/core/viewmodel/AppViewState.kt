package com.github.app.core.viewmodel

/**
 * Interface defining a View State within the application.
 * It is part of the base architecture alongside [AppViewModel].
 *
 * It provides a standardized way for [AppViewModel] to interact and update the ViewState in the app.
 */
interface AppViewState
